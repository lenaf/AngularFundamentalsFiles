'use strict';

//create a module called eventsApp
//store the module in a global variable to make it easier to create objects inside of module such as controllers and services. 
var eventsApp = angular.module('eventsApp', []);
